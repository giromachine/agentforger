# TOOLS.md - Local Notes

Skills define _how_ tools work. This file is for _your_ specifics — the stuff that's unique to your setup.

## Workspace tooling

### Validators

- Location: `agentforger/validators/`
- Self-check: `npm --prefix agentforger test`

### Directory structure

- Skills root: `skills/`
- Agents root: `agents/`

### Runtime

- Node.js ES modules (`"type": "module"`) — validators use `.mjs` extension
